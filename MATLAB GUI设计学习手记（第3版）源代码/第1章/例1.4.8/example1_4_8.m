N = 10;
a = randi([50 100], N, 100);

%%%%%%%以下代码段为循环方式实现查找%%%%%%%%%
num1 = ones(N, 1);
for i = 1 : N
    num1(i) = nnz(a(i, :) > 80);
end
num1

%%%%%%%以下代码段为矩阵运算方式实现查找%%%%%%%
% num2 = sum(a > 80, 2)
